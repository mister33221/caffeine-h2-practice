spring:
  application:
    name: cache-h2-practice
  #  h2
  datasource:
    url: jdbc:h2:mem:testdb # in-memory database, database name is testdb
    driver-class-name: org.h2.Driver # H2 driver
    username: sa # default username
    password: password # default password
  h2:
    console:
      enabled: true # enable H2 console, this console can be accessed by http://localhost:8080/h2-console
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    #    ddl- auto has 5 options: none, validate, update, create, create-drop
    #    none: ???????
    #    validate: ?? DB schema ??? Hibernate ? Entity schema ????????????????????????? production ???
    #    update: ?? Hibernate ? Entity schema ?? DB schema?????????????????????
    #    create: ?? Hibernate ? Entity schema ???? DB schema???????????
    #    create-drop: ? SessionFactory ???????? DB schema?????? test ???
    show-sql: true # ?? SQL ?????? console ?? SQL ??
    open-in-view: false # ???? Hibernate Session ????????? View ???????? LazyInitializationException?????????????????? false?
    generate-ddl: false # ? ddl-auto ????? ddl-auto ????????????hibernate.ddl-auto ????? spring.jpa.generate-ddl??????? ddl-auto?????? false?
    defer-datasource-initialization: true
#  ??? @ControllerAdvicer ?????????????????api?????????org.springframework.web.servlet.NoHandlerFoundException?????application.yaml?????????????????????????????????????
#  mvc:
#    throw-exception-if-no-handler-found: false
#  web:
#    resources:
#      add-mappings: false

# swagger
springdoc:
  swagger-ui:
    path: /swagger-ui.html
